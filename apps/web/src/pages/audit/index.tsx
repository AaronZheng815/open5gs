import { Card, Typography } from 'antd';

export function AuditPage() {
  return (
    <Card>
      <Typography.Title level={4}>审计</Typography.Title>
      <Typography.Text type="secondary">审计日志 / 生命周期任务（T-16 实现）</Typography.Text>
    </Card>
  );
}
