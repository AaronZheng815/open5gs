# NMS Console backend 配置示例（复制为 .env 使用；.env 不入库）
PORT=5000
MONGO_URI=mongodb://localhost/open5gs
# NRF SBI 地址（资产在线叠加使用，决策 2.3/2.6）
NRF_BASE_URL=http://127.0.0.10:7777
# JWT（T-5 auth；生产必须替换为强随机 secret）
JWT_SECRET=open5gs-nms-dev-secret
JWT_EXPIRES_IN=1d
